# Verbi modali · Ausiliari

Pagina di ripasso interattiva per studenti di italiano A2–B1: scelta di
`essere` / `avere` con *dovere*, *potere* e *volere* al passato prossimo.

## Anteprima locale

```bash
python3 -m http.server 8000
```

Poi apri <http://localhost:8000>.
